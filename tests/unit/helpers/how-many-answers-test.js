import { howManyAnswers } from 'js-week3-review/helpers/how-many-answers';
import { module, test } from 'qunit';

module('Unit | Helper | how many answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = howManyAnswers([42]);
  assert.ok(result);
});
